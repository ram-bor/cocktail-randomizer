import React from 'react';

export default function Button(props) {
  return (
    <div>
      <button
        type="button"
        className="btn btn-success btn-lg"
        // onClick={() => {}}
      >
        GO
      </button>
    </div>
  );
}
